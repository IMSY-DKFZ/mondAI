# Documentation

To build this documentation locally, run the following commands:
```bash
cd docs
make clean
make html
```

Then simply open the `index.html` file generated under `docs/build/html/`.

This documentation will be automatically build and deployed upon merge into the main branch.
